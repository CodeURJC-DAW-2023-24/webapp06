# DockerfileSpring para la aplicación Spring Boot
FROM openjdk:17-oracle

# Argumento para especificar la ubicación del archivo JAR desde el directorio target
ARG JAR_FILE=./target/backend-0.0.1-SNAPSHOT.jar

# Copia el JAR construido con Maven al contenedor
COPY ${JAR_FILE} app.jar

# Punto de entrada para ejecutar la aplicación Spring Boot
ENTRYPOINT ["java","-jar","/app.jar"]
